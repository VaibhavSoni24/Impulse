# IMPULSE — Local Harness Environment Record
# Generated: 2026-09-25T18:09:00Z
# Platform: Microsoft Windows 11 Home Single Language 10.0.26200 (x86_64)

[Python Runtime]
python_version = Python 3.13.15
python_executable = E:\Projects\Impulse\.venv\Scripts\python.exe
pip_version = pip 26.2.1

[Host Evaluation Packages]
# Note: swegemma, adk-submission, and adk-eval-core are proprietary competition packages
# pre-installed in the Kaggle scoring container; they are not published on public PyPI.
swegemma = 0.2.7 (competition evaluation harness, remote Kaggle runtime)
adk-submission = 0.2.11 (declarative agent compiler, remote Kaggle runtime)
adk-eval-core = 0.1.0 (evaluation task/result models, remote Kaggle runtime)
# Official compiler: adk-submission compile <submission_dir> (NOT EXECUTED LOCALLY; remote Kaggle image only)
# Local reproduction tool: python scripts/validate_submission.py <submission_dir> (VERIFIED executed on Windows)

[Inference Serving Engine]
# vLLM is managed by adk-submission on 4x NVIDIA L4 GPUs in the remote scoring environment.
# Note: Exact package wheel version and runtime version are NOT provided in competition assets.
# (0.19.1 and 0.18.0 were unverified placeholder inferences and are superseded by this audit).
vllm_package_wheel_version = NOT PROVIDED IN ASSETS (unverified)
vllm_runtime_serving_version = UNSPECIFIED IN ASSETS (unverified)
vllm_serving_config = VERIFIED (tensor_parallel_size=4, gpu_memory_utilization=0.90, max_model_len=32768, 4x L4)

[Local Package Installation State]
# In strict accordance with Stage 3 rules, no unverified packages have been installed from PyPI.
# The local .venv remains clean and minimal.
installed_packages:
  - pip==26.2.1

[Sandbox Wheelhouse Inventory]
wheelhouse_manifest = data/competition/wheels_manifest.json
total_wheels = 124
pure_python_wheels = 116
linux_specific_wheels = 8
wheelhouse_mount_point = /wheels/ (inside swebench-sandbox container)
